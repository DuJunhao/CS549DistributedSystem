# These properties are defined in ftp/pom.xml,
# 
# 
# Properties for the FTP Server
# 
server.name=${server.name}
server.machine=${server.machine}
server.port=${server.port}
client.ip=${client.internal-ip}
server.ip=${server.internal-ip}